# Uncomment the next line to define a global platform for your project
# platform :ios, '12.2'

target 'toio controller' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SwiftLint'
  pod 'SwiftFormat/CLI'
  pod 'LicensePlist'


  target 'toio controllerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'toio controllerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
